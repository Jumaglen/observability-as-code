apiVersion: 1

# Provision dashboards from the dashboards directory
providers:
  - name: 'Dashboard Provider'
    orgId: 1
    folder: 'Observability'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: false
    options:
      path: /etc/grafana/provisioning/dashboards
